{
    "tasks": [
        { 
            "type": "shell",
            "label": "C/C++: gcc.exe build active file",
            "command": "C:\\Win-builds-64\\bin\\gcc.exe",
            "args": [
                "-g",
                "${file}",
                "memory.ico",
                "-o",
                "${fileDirname}\\${fileBasenameNoExtension}.exe"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ],
    "version": "2.0.0"
}